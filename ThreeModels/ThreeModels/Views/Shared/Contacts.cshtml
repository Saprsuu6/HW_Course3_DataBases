@model Contacts

<dl class="dl-horizontal">
    <dt>Eamil</dt>
    <dd>@Model.Email</dd>

    <dt>Phones</dt>
    @foreach(Phone phone in Model.Phones)
    {
        <dd>@phone.Number</dd>
    }

    <dt>Location</dt>
    <dd>@Model.CountryTownStreet</dd>
</dl>
